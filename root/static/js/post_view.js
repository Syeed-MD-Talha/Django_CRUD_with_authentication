// You can add JavaScript functionality if needed
// For example, to dynamically load blog posts from an API
document.addEventListener('DOMContentLoaded', () => {
    console.log('Blog post cards loaded');
});
